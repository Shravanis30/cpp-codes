#include <bits/stdc++.h>
using namespace std;

int main()
{
    char P[] = ("Shraavniii");
    char Q[] = ("aavniii");
    int k = 1;
    int R = strlen(P);
    int S = strlen(Q);
    int Max = R - S + 1;
    while(k <= Max)
    {
        for(int L = 1; L <= R; L++)
        {
            // P[L] != T[k+L-1];
        }
    }
    return 0;
}